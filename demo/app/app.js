/**
 * @author Milad Naseri (mmnaseri@programmer.net)
 * @since 1.0 (4/8/16)
 */
/**
 * AngularJS module for demoing the component
 * @type {angular.Module}
 */
var module = angular.module('protonMultiListSelector', []);

module.run(function () {
    
});

module.controller("MainController", function ($scope) {
    $scope.model = {};
});